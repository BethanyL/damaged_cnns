function net = setup_experiment(net_file_name, face_flag)

% this function may seem silly, but in the Python version, it also needs to do same data setup 

net = setup_network(net_file_name, face_flag);
